"use client";
import { FormEvent, useEffect, useState } from "react";
import { CheckCircle2, Search, Wrench } from "lucide-react";
export default function TrackPage(){const[data,setData]=useState<any>(null);const[error,setError]=useState("");async function find(id:string){setError("");setData(null);const r=await fetch(`/api/public/track?id=${encodeURIComponent(id)}`);const j=await r.json();if(!r.ok)setError(j.error);else setData(j.ticket)}useEffect(()=>{const id=new URLSearchParams(location.search).get("id");if(id)find(id)},[]);function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();find(String(new FormData(e.currentTarget).get("id")))}return <main className="trackPage"><section className="trackCard"><span className="authLogo"><Wrench/></span><h1>Cek Status Servis</h1><p>Masukkan nomor servis atau scan QR pada tanda terima.</p><form onSubmit={submit}><input name="id" required placeholder="Contoh: SRV-260702-016"/><button><Search/> Cari</button></form>{error&&<div className="authError">{error}</div>}{data&&<div className="trackResult"><CheckCircle2/><small>No. Servis</small><strong>{data.id}</strong><h2>{data.device}</h2><i>{data.status}</i><dl><div><dt>Diterima</dt><dd>{data.receivedAt}</dd></div><div><dt>Terakhir diperbarui</dt><dd>{data.updatedAt}</dd></div><div><dt>Tindakan</dt><dd>{data.serviceAction||"Belum ada tindakan"}</dd></div></dl></div>}</section></main>}
